import { LiveAddressDirective } from './live-address.directive';

describe('LiveAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveAddressDirective();
    expect(directive).toBeTruthy();
  });
});
